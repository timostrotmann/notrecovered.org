---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { withLocale } from '../utils/i18n';

interface Props {
	title: string;
	description: string;
	alternateHref?: string;
}

const { title, description, alternateHref } = Astro.props as Props;
const switchHref = alternateHref ?? withLocale(Astro.url.pathname, 'de');
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={title} description={description} locale="en" alternateHref={switchHref} />
	</head>
	<body>
		<Header locale="en" alternateHref={switchHref} />
		<slot />
		<Footer locale="en" />
	</body>
</html>
